services:
  claim-auto:
    build: .
    image: claim-auto:latest
    container_name: claim-auto
    restart: unless-stopped
    # Laisse le temps de fermer proprement le navigateur : sinon une session de
    # login en cours est tuée avant que Chrome n'ait écrit ses cookies.
    stop_grace_period: 45s
    # Écrit dans ./data avec ton UID plutôt qu'en root.
    user: "${PUID:-1000}:${PGID:-1000}"
    env_file:
      - .env
    environment:
      HOME: /data/home
    ports:
      # Port côté hôte surchargeable dans .env s'il est déjà utilisé.
      - "${WEB_PORT_HOST:-8080}:8080"     # interface web
      # noVNC (6080) et VNC brut (5900) ne sont volontairement PAS publiés :
      # ils n'ont aucune authentification, alors que le VNC servi par
      # l'interface web est protégé comme le reste. Les décommenter contourne
      # cette protection — à faire en connaissance de cause.
      # - "${NOVNC_PORT_HOST:-6080}:6080"
      # - "${VNC_PORT_HOST:-5900}:5900"
    volumes:
      - ./data:/data
    shm_size: "1gb"   # sinon Firefox/Chromium crashent sur les grosses pages
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8080/api/status').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 20s

  # Test / usage ponctuel — ne tourne pas en permanence :
  #   docker compose run --rm test detect        (juste voir ce qui est dispo)
  #   docker compose run --rm test run epic      (claim d'un seul store)
  test:
    build: .
    image: claim-auto:latest
    profiles: ["cli"]
    # Écrit dans ./data avec ton UID plutôt qu'en root.
    user: "${PUID:-1000}:${PGID:-1000}"
    env_file:
      - .env
    environment:
      HOME: /data/home
    volumes:
      - ./data:/data
    shm_size: "1gb"
    entrypoint: ["/usr/bin/tini", "--", "/usr/local/bin/docker-entrypoint.sh", "node", "src/index.js"]
    command: ["detect"]
